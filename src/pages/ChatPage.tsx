import * as React from "react";
import BasicModal from "../components/Model";
export default function ChatPage() {

  return (
    <div>
      <BasicModal buttonText="start whispering" open={true} setOpen={()=>{}} title="WhisperAI" description=""></BasicModal>
    </div>
  );
}
